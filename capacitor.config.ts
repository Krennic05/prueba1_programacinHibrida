import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.felixortizprueba1',
  appName: 'felixortizprueba1',
  webDir: 'www'
};

export default config;
